name: InfoSec Relations - Offensive Engineering - Hello World!

on: workflow_dispatch

jobs:
  hello-world:
    runs-on: ubuntu-latest
    name: Job
    steps:
      - run: id
      - run: uname -a
      - run: env
      - run: echo "Hello InfoSec Relations - Offensive Engineering!"

