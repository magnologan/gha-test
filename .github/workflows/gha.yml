on: [push]

jobs:
  Nmap:
    runs-on: ubuntu-latest
    name: Nmap
    steps:
      - run: |
            sudo apt install nmap -y
      - run: |
            nmap -sV -p8084 --script default,safe,exploit --resolve-all localhost
