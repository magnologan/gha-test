on: [push]

jobs:
  Nmap:
    runs-on: ubuntu-latest
    name: OWASP
    steps:
      - run: |
            id
            uname -a
            whoami
            ls -la
            env
            echo "Hello OWASP Vancouver!"
      
